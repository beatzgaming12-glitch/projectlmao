# Host a public .properties file and point this URL at it.
# The app checks it on startup and shows an optional update banner if a newer version exists.
app.version=${version}
update.metadataUrl=https://example.com/projectlmao/latest.properties
